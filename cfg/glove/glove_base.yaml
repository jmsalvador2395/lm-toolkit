
general : 
    device : "cuda:1"
    seed : 123
    base_logdir : "{{project_root}}/tensorboard"
    note : "a quick note about this training run. this note will show up in tensorboard."

model :
    name : "glove"
    embedding_dim : 200
    checkpoint_path : Null
    vocab_size : 150000
    weight_decay : 0
    alpha_weight : .0001
    ckpt_dir : "{{project_root}}/checkpoints"
    weight_decay : 0

task :
    window_size : 10
    min_freq : 100

data : 
    num_proc : 20
    num_shards : 10
    cache_dir : "{{home}}/ds_cache"
    shuffle : True
    batch_size : 4096
    num_epochs : 10
    eval_freq : 1000
